using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RotateGunScript : MonoBehaviour
{
    //   private void RotateGun()
    // {
    //     if (isGrappling || isGrappleDashing)
    //     {
    //         gunDirection = grappleDirectionVector;
    //         float angle = Mathf.Atan2(gunDirection.y, gunDirection.x) * Mathf.Rad2Deg;
    //         gunPivot.rotation = Quaternion.AngleAxis(angle, Vector3.forward);
    //     }
    //     else
    //     {
    //         gunDirection = playerScript.inputScript.moveDirection == Vector2.zero? 
    //             gunDirection : playerScript.inputScript.moveDirection;   
    //         float angle = Mathf.Atan2(gunDirection.y, gunDirection.x) * Mathf.Rad2Deg;
    //         gunPivot.rotation = Quaternion.AngleAxis(playerScript.animationScript.isFacingRight?
    //             angle : 180 + angle, Vector3.forward);
    //     }
    // }
}
